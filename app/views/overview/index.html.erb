<h1> <%= heroicon "magnifying-glass", options: { class: "h-6 w-6" } %> hey <%= current_user.name %></h1>

<ul>
    <% current_user.recent_pokemons.each do |pokemon| %>
    <li>
        <%= pokemon.name %>
    </li>
    <% end %>
 </ul>